using System;
using System.Collections.Generic;
using System.Text;

namespace Telegram.Altayskaya97.Bot.Helpers
{
    public static class IdMaker
    {
        /*
        public static long MakeMessageId(long chatId, int messageId)
        {
            string sChatId = chatId.ToString();
            string sMessageId = messageId.ToString();
            string sId = sChatId + sMessageId;
            return long.Parse(sId);
        }

        public static int GetTelegramMessageId(long messageId, long chatId)
        {
            string sMessageId = messageId.ToString();
            string sChatId = chatId.ToString();
            string sTelegramMessageId = sMessageId.Replace(sChatId, "");
            return int.Parse(sTelegramMessageId);
        }
        */
    }
}
